# نواة — دليل المشروع الشامل

> منصة مؤسسية عربية لإنشاء وإدارة وتوزيع التوكنات الرقمية (ERC-20) عبر شبكات EVM المتعددة، بدون كتابة كود.

---

## فهرس المحتويات

1. [نظرة عامة على المشروع](#١-نظرة-عامة-على-المشروع)
2. [هيكل المجلدات](#٢-هيكل-المجلدات)
3. [أقسام المنصة — تفصيل كامل](#٣-أقسام-المنصة--تفصيل-كامل)
4. [التقنيات المستخدمة](#٤-التقنيات-المستخدمة)
5. [تشغيل المشروع محلياً](#٥-تشغيل-المشروع-محلياً)
6. [التوسعة المستقبلية](#٦-التوسعة-المستقبلية)

---

## ١. نظرة عامة على المشروع

**نواة** هي منصة SaaS مؤسسية موجهة للشركات والمؤسسات العربية التي تريد:

- **إصدار التوكنات**: إنشاء عقود ERC-20 ذكية بخطوات مرئية دون كتابة Solidity
- **إدارة المحافظ**: ربط محافظ متعددة وتشفير مفاتيحها الخاصة
- **التوزيع الجماعي**: إرسال التوكنات لآلاف العناوين عبر ملفات CSV أو إدخال يدوي
- **مراقبة الأصول**: تتبع كل عقد منشور ومعاملاته في الوقت الفعلي
- **التدقيق والامتثال**: سجل غير قابل للتعديل لكل إجراء في المنصة
- **إدارة الفريق**: صلاحيات RBAC دقيقة لكل عضو في المؤسسة

**الجمهور المستهدف**: مديرو الخزانة، فرق التمويل المؤسسي، مشاريع Web3 العربية، والشركات الناشئة في مجال DeFi.

---

## ٢. هيكل المجلدات

```
token-forge/
├── src/
│   ├── components/
│   │   ├── AppShell.tsx        ← الهيكل الرئيسي (sidebar + header)
│   │   ├── Primitives.tsx      ← مكونات مشتركة (PageHeader, StatusPill)
│   │   └── ui/                 ← مكونات shadcn/ui الجاهزة
│   ├── pages/
│   │   ├── Dashboard.tsx       ← لوحة التحكم
│   │   ├── TokenFactory.tsx    ← مصنع التوكنات
│   │   ├── Wallets.tsx         ← إدارة المحافظ
│   │   ├── Distribution.tsx    ← التوزيع الجماعي
│   │   ├── Assets.tsx          ← سجل الأصول
│   │   ├── Networks.tsx        ← إعدادات الشبكات
│   │   ├── Reports.tsx         ← التقارير والتحليلات
│   │   ├── Users.tsx           ← إدارة المستخدمين
│   │   ├── Audit.tsx           ← سجلات التدقيق
│   │   └── Settings.tsx        ← إعدادات النظام
│   ├── routes/
│   │   ├── __root.tsx          ← الجذر (HTML shell + error/404)
│   │   ├── _app.tsx            ← layout لصفحات التطبيق
│   │   ├── auth.tsx            ← layout صفحات المصادقة
│   │   ├── _app.index.tsx      ← / لوحة التحكم
│   │   ├── _app.factory.tsx    ← /factory
│   │   ├── _app.wallets.tsx    ← /wallets
│   │   ├── _app.distribution.tsx ← /distribution
│   │   ├── _app.assets.tsx     ← /assets
│   │   ├── _app.assets.$id.tsx ← /assets/:id تفاصيل التوكن
│   │   ├── _app.networks.tsx   ← /networks
│   │   ├── _app.reports.tsx    ← /reports
│   │   ├── _app.users.tsx      ← /users
│   │   ├── _app.audit.tsx      ← /audit
│   │   ├── _app.settings.tsx   ← /settings
│   │   ├── auth.login.tsx      ← /auth/login
│   │   ├── auth.register.tsx   ← /auth/register
│   │   └── auth.forgot.tsx     ← /auth/forgot
│   ├── routeTree.gen.ts        ← مولّد تلقائي لشجرة المسارات
│   ├── router.tsx              ← إعداد TanStack Router + QueryClient
│   ├── main.tsx                ← نقطة دخول React
│   └── styles.css              ← المتغيرات والثيمات (Tailwind v4)
├── PROJECT_GUIDE.md            ← هذا الملف
├── package.json
├── vite.config.ts
└── tsconfig.json
```

---

## ٣. أقسام المنصة — تفصيل كامل

---

### 🏠 لوحة التحكم — `/`
**الملف**: `src/pages/Dashboard.tsx`

**الغرض**: نقطة البداية لكل مستخدم عند تسجيل الدخول. تعطي نظرة شاملة فورية على صحة المنصة.

**ما يعرضه:**
| العنصر | الوصف |
|--------|-------|
| بطاقات KPI | التوكنات المنشورة، إجمالي التحويلات، المحافظ النشطة، العمليات المعالجة |
| مخطط التحويلات | رسم بياني خطي لنشاط 30 يوماً الماضية (Recharts) |
| تنبيهات النظام | آخر الأحداث الأمنية والتشغيلية |
| جدول التوكنات | أسرع وصول للأصول الأكثر نشاطاً |

**الفائدة للمؤسسة**: يوفر رؤية تنفيذية فورية دون الحاجة للتعمق في التقارير.

---

### 🏭 مصنع التوكنات — `/factory`
**الملف**: `src/pages/TokenFactory.tsx`

**الغرض**: إنشاء عقود ERC-20 ذكية بطريقة مرئية خطوة بخطوة، دون كتابة أي كود Solidity.

**خطوات الإنشاء:**
1. **المعلومات الأساسية**: اسم التوكن، الرمز (Symbol)، المنازل العشرية، المعروض الأولي
2. **الشبكة والمحفظة**: اختيار شبكة EVM والمحفظة الراسلة
3. **الخصائص الاختيارية**: Mintable / Burnable / Pausable / Capped Supply
4. **المراجعة**: ملخص كامل + تقدير تكلفة الغاز قبل التأكيد
5. **النشر**: مراقبة مراحل النشر (ترجمة → تقدير → توقيع → نشر)
6. **النجاح**: عنوان العقد المنشور مع رابط للمستكشف

**الفائدة**: يُمكّن فريق الخزانة من إصدار التوكنات مستقلاً دون الاعتماد على مطورين خارجيين.

---

### 💼 إدارة المحافظ — `/wallets`
**الملف**: `src/pages/Wallets.tsx`

**الغرض**: مركزية إدارة المحافظ المؤسسية المرتبطة بالمنصة.

**الوظائف:**
- عرض جميع المحافظ مع أرصدتها الحالية
- إضافة محافظ جديدة (بمفتاح خاص مشفر بـ Fernet)
- نسخ العناوين بنقرة واحدة
- أرشفة / تعطيل المحافظ غير المستخدمة
- عرض حالة كل محفظة (نشطة / مجمدة / أرشيف)

**الأمان**: المفتاح الخاص يُشفَّر ولا يُعرض أبداً بعد الحفظ.

---

### 📤 مركز التوزيع — `/distribution`
**الملف**: `src/pages/Distribution.tsx`

**الغرض**: إرسال التوكنات بكميات كبيرة لآلاف العناوين في وقت واحد.

**وضعا التوزيع:**

| الوضع | الاستخدام |
|-------|-----------|
| **تحويل فردي** | إرسال مبلغ محدد لعنوان واحد مع مذكرة |
| **توزيع جماعي CSV** | رفع ملف CSV (عنوان + مبلغ) للتوزيع الكبير |

**سير العمل الجماعي:**
1. تحميل قالب CSV
2. رفع الملف والتحقق من البيانات
3. مراجعة الملخص (عدد العناوين، إجمالي التوكنات، تكلفة الغاز)
4. تأكيد وإرسال الدفعة
5. متابعة في سجل الدفعات

**الفائدة**: مثالي لتوزيع رواتب التوكنات، مكافآت المستخدمين، أو توزيع الأسهم الرقمية.

---

### 📊 سجل الأصول — `/assets` و `/assets/:id`
**الملفات**: `src/pages/Assets.tsx`، `src/routes/_app.assets.$id.tsx`

**الغرض**: قائمة شاملة بكل التوكنات المنشورة مع إمكانية البحث والتصفية.

**قدرات التصفية:**
- بحث نصي: اسم التوكن، الرمز، عنوان العقد
- فلتر الشبكة: Ethereum / Polygon / Arbitrum / BNB / Base
- فلتر الحالة: نشط / قيد الانتظار / فشل

**صفحة تفاصيل التوكن** (`/assets/:id`):
- معلومات العقد الكاملة (عنوان، شبكة، معيار، تاريخ نشر)
- إحصائيات: الحاملون، عدد التحويلات
- قائمة آخر التحويلات
- أزرار إجراء: سك توكنات إضافية / إيقاف العقد
- رابط مباشر للـ Etherscan / Block Explorer

---

### 🌐 إعدادات الشبكات — `/networks`
**الملف**: `src/pages/Networks.tsx`

**الغرض**: إدارة نقاط اتصال RPC بشبكات EVM المختلفة على مستوى المؤسسة.

**الوظائف:**
| الوظيفة | التفاصيل |
|---------|---------|
| إضافة شبكة | اسم، Chain ID، RPC URL، العملة الأصلية، رابط المستكشف |
| تعديل شبكة | تحديث بيانات الاتصال |
| اختبار الاتصال | فحص فوري مع عرض زمن الاستجابة ورقم الكتلة |
| تفعيل/تعطيل | التحكم في الشبكات المتاحة للمستخدمين |
| حذف شبكة | إزالة شبكات غير مستخدمة |

**لماذا نحتاجها**: المؤسسات غالباً تستخدم شبكات خاصة (private chains) أو RPC خاص بها مع API keys، هذا القسم يسمح بإدارتها مركزياً.

---

### 📈 التقارير — `/reports`
**الملف**: `src/pages/Reports.tsx`

**الغرض**: توليد تقارير تحليلية دورية لمتابعة أداء منظومة التوكنات.

**أنواع التقارير:**
- ملخص التوكنات الشهري (PDF)
- تقرير حركة التحويلات (Excel)
- تقرير الامتثال والتدقيق
- تحليل الحاملين وتوزيعهم

**الفائدة**: تسهّل تقديم التقارير للإدارة العليا ومتطلبات الامتثال التنظيمي.

---

### 👥 إدارة المستخدمين — `/users`
**الملف**: `src/pages/Users.tsx`

**الغرض**: إدارة أعضاء الفريق وصلاحياتهم وفق نظام RBAC.

**الأدوار المتاحة:**

| الدور | الصلاحيات |
|-------|-----------|
| **مدير عام** | جميع الصلاحيات بلا قيود |
| **مدير مالي** | التحويلات، التقارير المالية، إدارة المحافظ |
| **مدير عمليات** | نشر التوكنات، إدارة الشبكات، التوزيع الجماعي |
| **مدقق** | قراءة سجلات التدقيق والتقارير فقط |
| **مشاهد** | قراءة محدودة للوحة التحكم فقط |

**وظائف الصفحة:**
- دعوة أعضاء جدد عبر البريد المؤسسي
- تغيير دور أي عضو بشكل فوري
- تفعيل / تعطيل الحسابات
- عرض لوحة تفاصيل جانبية لكل مستخدم
- إزالة المستخدمين

---

### 🛡️ سجلات التدقيق — `/audit`
**الملف**: `src/pages/Audit.tsx`

**الغرض**: سجل غير قابل للتعديل يتتبع كل إجراء حساس في المنصة للامتثال والأمان.

**مستويات الأحداث:**
| المستوى | اللون | الأمثلة |
|---------|------|---------|
| **INFO** | رمادي | نشر عقد، تسجيل دخول ناجح، تصدير تقرير |
| **WARN** | أصفر | محاولة دخول فاشلة، تغيير صلاحيات |
| **CRIT** | أحمر | تجميد محفظة، محاولة وصول غير مصرح |

**قدرات التصفية:**
- بحث نصي حر (العملية، الكيان، المستخدم، IP)
- فلتر المستوى (كليك على بطاقة الإحصاء)
- فلتر المستخدم
- تصدير CSV للسجلات

**الفائدة**: يفي بمتطلبات ISO 27001 وأنظمة الامتثال المالي التي تطلب audit trail كاملاً.

---

### ⚙️ الإعدادات — `/settings`
**الملف**: `src/pages/Settings.tsx`

**الغرض**: ضبط المنصة على مستوى المؤسسة.

**التبويبات:**
| التبويب | المحتوى |
|---------|---------|
| **معلومات المؤسسة** | اسم الشركة، البريد، الشعار، المنطقة الزمنية |
| **الشبكات** | اختصار للشبكات المُدارة (رابط لـ /networks) |
| **المستخدمون** | اختصار لإدارة المستخدمين (رابط لـ /users) |
| **الأمان** | 2FA، سياسة كلمة المرور، إدارة Sessions |
| **النسخ الاحتياطي** | جدولة النسخ وتشفيرها |
| **الفواتير** | الاشتراك الحالي والفواتير السابقة |

---

### 🔐 المصادقة — `/auth/*`
**الملفات**: `src/routes/auth.*.tsx`

**الصفحات:**

| الصفحة | المسار | الوصف |
|---------|-------|-------|
| تسجيل الدخول | `/auth/login` | بريد + كلمة مرور + SSO مؤسسي |
| إنشاء حساب | `/auth/register` | معالج 3 خطوات: معلومات الشركة → حساب المسؤول → اختيار الخطة |
| نسيت كلمة المرور | `/auth/forgot` | إرسال رابط استعادة آمن |

**الـ Layout**: تصميم ثنائي الشريط — يسار: هوية المنصة ومزاياها، يمين: نموذج المصادقة.

---

## ٤. التقنيات المستخدمة

| التقنية | الدور | السبب |
|---------|-------|-------|
| **React 19** | UI Framework | أحدث إصدار مع Server Components |
| **Vite 7** | Build Tool | أسرع أداة بناء وتطوير |
| **TypeScript 5.9** | Type Safety | منع الأخطاء وتوثيق الكود |
| **TanStack Router** | File-based Routing | Type-safe routing مع lazy loading |
| **TanStack Query** | Server State | Caching وإعادة المحاولة التلقائية |
| **Tailwind CSS v4** | Styling | CSS variables مع design tokens |
| **shadcn/ui** | UI Components | مكونات قابلة للتخصيص كاملاً |
| **Radix UI** | Headless Components | Accessibility مدمجة |
| **Recharts** | Charts | رسوم بيانية تفاعلية |
| **Lucide React** | Icons | 1000+ أيقونة متسقة |
| **Sonner** | Toast Notifications | إشعارات أنيقة خفيفة |
| **IBM Plex Sans Arabic** | Typography | خط عربي مؤسسي احترافي |
| **JetBrains Mono** | Monospace | عرض العناوين والأكواد |

---

## ٥. تشغيل المشروع محلياً

```bash
# استنساخ المشروع
git clone https://github.com/akr-2009-haig/token-forge.git
cd token-forge

# تثبيت التبعيات
npm install
# أو
pnpm install

# تشغيل خادم التطوير
npm run dev
# يفتح على http://localhost:5173

# بناء للإنتاج
npm run build

# معاينة البناء النهائي
npm run preview
```

**متطلبات البيئة:**
- Node.js 18 أو أحدث
- npm 9+ أو pnpm 8+
- لا يوجد backend — الواجهة كاملة وستاتيك

---

## ٦. التوسعة المستقبلية

### قريباً (أولوية عالية)
- [ ] **Backend API**: Express + Drizzle ORM + PostgreSQL لتخزين البيانات الفعلية
- [ ] **تكامل Wagmi/viem**: اتصال حقيقي بشبكات EVM وتوقيع المعاملات
- [ ] **نشر عقود حقيقي**: تكامل مع Hardhat / Foundry لنشر Solidity فعلي
- [ ] **المصادقة الفعلية**: JWT + refresh tokens + SSO/SAML المؤسسي
- [ ] **الإشعارات الفورية**: WebSocket للتحديثات الحية

### مستقبلاً (أولوية متوسطة)
- [ ] **تعدد اللغات (i18n)**: دعم الإنجليزية والفرنسية بجانب العربية
- [ ] **الوضع الداكن التلقائي**: يتبع تفضيلات النظام
- [ ] **لوحة Analytics متقدمة**: DeFi metrics، TVL، holder analytics
- [ ] **تكامل Chainlink**: بيانات أسعار Oracle
- [ ] **نظام Webhook**: إشعارات للأنظمة الخارجية عند الأحداث
- [ ] **تطبيق موبايل**: React Native للمديرين

### البنية التحتية
- [ ] نشر على Vercel / AWS
- [ ] CI/CD pipeline مع GitHub Actions
- [ ] اختبارات E2E مع Playwright
- [ ] مراقبة الأخطاء مع Sentry

---

## ملاحظات تقنية مهمة

### RTL (Right-to-Left)
المنصة مبنية RTL بالكامل:
- `<html lang="ar" dir="rtl">` في جذر الصفحة
- جميع layouts تستخدم `dir="rtl"`
- القيم العددية والعناوين تستخدم class `ltr` لعرضها يساراً لأنها Latin

### نظام الثيمات
```css
/* styles.css */
:root {
  --accent: oklch(0.68 0.22 38); /* برتقالي نواة */
  --background: oklch(0.98 0 0);
  --foreground: oklch(0.08 0 0);
}
.dark {
  --background: oklch(0.08 0 0);
  --foreground: oklch(0.95 0 0);
}
```

### إضافة صفحة جديدة
```bash
# 1. أنشئ ملف الصفحة
src/pages/MyPage.tsx

# 2. أنشئ ملف المسار
src/routes/_app.mypage.tsx

# 3. ملف المسار يحتوي على:
import { createFileRoute } from "@tanstack/react-router";
import { MyPage } from "@/pages/MyPage";
export const Route = createFileRoute("/_app/mypage")({
  component: MyPage,
});

# 4. أضف المسار في routeTree.gen.ts (أو شغّل vite dev ليُولَّد تلقائياً)
# 5. أضف الرابط في nav داخل AppShell.tsx
```

---

*آخر تحديث: مايو 2026 — نواة v1.0*
